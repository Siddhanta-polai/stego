# Steganography Backend API

FastAPI backend for adaptive pixel steganography with edge preservation.  
Uses MongoDB GridFS for image storage, AES‑GCM encryption, and key‑dependent embedding.

## Local Development

1. Install MongoDB and start it.
2. Create virtual environment and install dependencies:
   ```bash
   pip install -r requirements.txt